number = { ASCII_DIGIT+ }
//sum = { number ~ " + " ~ number }
//minus = { number ~ " - " ~ number }

add = {"+"}
subtract = {"-"}
operator = {add | subtract}
formula = {" "* ~"=" ~ " "*  ~ number ~ " "* ~ operator ~ " "* ~ number}

//formula = { SOI  ~ "=" ~ number | operator ~ EOI}
//constant = BOF + number + EOF
//full = constant | formula